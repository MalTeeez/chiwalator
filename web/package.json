{
  "name": "web",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun tsc && mkdir -p ./dist/ && cp src/index.html src/styles.css ../word_map.json ./dist/"
  }
}
